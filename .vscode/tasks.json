{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Demo: run normally",
      "type": "shell",
      "command": "python -m worker --request-json \"$(cat examples/refund-request.json)\"",
      "problemMatcher": []
    },
    {
      "label": "Retrace: record a fresh execution",
      "type": "shell",
      "command": "mkdir -p recordings/live && retracepython --recording recordings/live/my-run.retrace -m worker --request-json \"$(cat examples/refund-request.json)\"",
      "problemMatcher": []
    }
  ]
}
